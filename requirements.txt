# Pinned loosely here; notebooks pin exact versions in cell 1 and log them to
# reports/<name>.json for reproducibility (AGENTS.md §3.4).
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.65.0
matplotlib>=3.7.0
# GBM option for g_theta (Phase 2 only; not imported before the gate)
lightgbm>=4.0.0
